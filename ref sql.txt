-- Users table
CREATE TABLE users (
    id SERIAL PRIMARY KEY,
    name VARCHAR(255) NOT NULL,
    email VARCHAR(255) UNIQUE NOT NULL,
    password_hash VARCHAR(255),
    face_registered BOOLEAN DEFAULT FALSE,
    voice_registered BOOLEAN DEFAULT FALSE,
    face_embedding TEXT,
    voice_data TEXT,
    created_at TIMESTAMP DEFAULT NOW()
);

-- Accounts table
CREATE TABLE accounts (
    id SERIAL PRIMARY KEY,
    user_id INTEGER UNIQUE NOT NULL REFERENCES users(id),
    account_number VARCHAR(20) UNIQUE,
    balance NUMERIC DEFAULT 0,
    created_at TIMESTAMP DEFAULT NOW()
);

-- User PINs table
CREATE TABLE user_pins (
    id SERIAL PRIMARY KEY,
    user_id INTEGER UNIQUE NOT NULL REFERENCES users(id),
    pin_hash VARCHAR(255) NOT NULL,
    created_at TIMESTAMP DEFAULT NOW()
);

-- Beneficiaries table
CREATE TABLE beneficiaries (
    id SERIAL PRIMARY KEY,
    user_id INTEGER NOT NULL REFERENCES users(id),
    name VARCHAR(255) NOT NULL,
    account_number VARCHAR(20) NOT NULL,
    ifsc VARCHAR(11) NOT NULL,
    created_at TIMESTAMP DEFAULT NOW()
);

-- Transactions table
CREATE TABLE transactions (
    id SERIAL PRIMARY KEY,
    user_id INTEGER NOT NULL REFERENCES users(id),
    type VARCHAR(10) NOT NULL,
    amount NUMERIC NOT NULL,
    to_account VARCHAR(20),
    status VARCHAR(20),
    reference_id VARCHAR(50),
    timestamp TIMESTAMP DEFAULT NOW()
);

-- Login History table
CREATE TABLE login_history (
    id SERIAL PRIMARY KEY,
    user_id INTEGER NOT NULL REFERENCES users(id),
    login_method VARCHAR(20),
    ip_address VARCHAR(45),
    user_agent TEXT,
    timestamp TIMESTAMP DEFAULT NOW()
);





1.users

| column_name      | data_type                   | is_nullable | column_default                    |
| ---------------- | --------------------------- | ----------- | --------------------------------- |
| id               | integer                     | NO          | nextval('users_id_seq'::regclass) |
| username         | character varying           | NO          | null                              |
| email            | character varying           | NO          | null                              |
| face_embedding   | ARRAY                       | YES         | null                              |
| created_at       | timestamp without time zone | YES         | CURRENT_TIMESTAMP                 |
| voice_registered | boolean                     | YES         | false                             |
| voice_data       | jsonb                       | YES         | null                              |
| face_registered  | boolean                     | YES         | false                             |
| otp              | character varying           | YES         | null                              |
| otp_expires      | timestamp without time zone | YES         | null                              |



2.acccounts

| column_name    | data_type         | is_nullable | column_default                       |
| -------------- | ----------------- | ----------- | ------------------------------------ |
| id             | integer           | NO          | nextval('accounts_id_seq'::regclass) |
| user_id        | integer           | YES         | null                                 |
| account_number | character varying | NO          | null                                 |
| balance        | numeric           | YES         | 0                                    |
| ifsc           | character varying | YES         | null                                 |

3.beneficiaries

| column_name    | data_type         | is_nullable | column_default                            |
| -------------- | ----------------- | ----------- | ----------------------------------------- |
| id             | integer           | NO          | nextval('beneficiaries_id_seq'::regclass) |
| user_id        | integer           | YES         | null                                      |
| name           | character varying | NO          | null                                      |
| account_number | character varying | NO          | null                                      |
| ifsc           | character varying | NO          | null                                      |


4.login_history:


| column_name        | data_type                   | is_nullable | column_default                            |
| ------------------ | --------------------------- | ----------- | ----------------------------------------- |
| id                 | integer                     | NO          | nextval('login_history_id_seq'::regclass) |
| user_id            | integer                     | YES         | null                                      |
| timestamp          | timestamp without time zone | YES         | CURRENT_TIMESTAMP                         |
| auth_method        | character varying           | YES         | null                                      |
| success            | boolean                     | YES         | null                                      |
| ip_address         | character varying           | YES         | null                                      |
| verification_score | double precision            | YES         | null                                      |
| similarity_score   | double precision            | YES         | null                                      |
| biometric_score    | double precision            | YES         | null                                      |



5.transactions:

| column_name  | data_type                   | is_nullable | column_default                           |
| ------------ | --------------------------- | ----------- | ---------------------------------------- |
| id           | integer                     | NO          | nextval('transactions_id_seq'::regclass) |
| user_id      | integer                     | YES         | null                                     |
| type         | character varying           | YES         | null                                     |
| amount       | numeric                     | YES         | null                                     |
| to_account   | character varying           | YES         | null                                     |
| status       | character varying           | YES         | null                                     |
| reference_id | character varying           | YES         | null                                     |
| timestamp    | timestamp without time zone | YES         | CURRENT_TIMESTAMP                        |


6.user_pins


| column_name | data_type         | is_nullable | column_default |
| ----------- | ----------------- | ----------- | -------------- |
| user_id     | integer           | NO          | null           |
| pin_hash    | character varying | NO          | null           |
